# Changelog

## [Unreleased]

### 移除
- ⚙ 面板的「開啟設定」按鈕 — iOS 27 起鍵盤 extension 沒有任何能開容器 app 的路（SwiftUI Link、
  extensionContext.open、EnvironmentValues.openURL 全部失效），按了只會跳「無法自動開啟」；
  設定請從主畫面開「OhMyBias 米」
- 字頻學習（FreqTracker）— 同碼字依使用習慣自動排序與 `,,RS` 重置，連同 freq.db、SQLite 與 iCloud
  合併一併移除：那是鍵盤一啟動就常駐、又不在任何釋放路徑上的三張字典，換來的只有同碼字的順序。
  候選順序回到 liu.cin 原序；`,,PIN`／`,,UNPIN` 固定排序保留，改存 App Group 的 pinned.txt
  （與 Android 版同格式）— 之前存在 freq.db 裡的固定排序需重設一次

## [0.5.1] - 2026-09-03

### 修正
- 安裝／首次啟動／匯入 liu.cin 後預設輸入法回到嘸蝦米（米）— 之前在還沒字表時切到英文的狀態會永遠留下來，
  匯入字表後鍵盤仍是英文；鍵盤行程還活著時也會即時重載剛匯入的字表與中英狀態，不必等系統重啟鍵盤

## [0.5.0] - 2026-09-02

### 修正
- 「匯入 liu.cin」點了沒反應 — 同一頁兩個檔案選擇器只有一個會彈出；iCloud／雲端硬碟尚未下載的
  檔案改走協調讀取；支援 Big5、UTF-16、帶 BOM 的 .cin；匯入改背景執行、失敗不動現有字表、
  錯誤訊息說明是哪一步失敗
- 面板「記憶體不足」誤判 — 記憶體上限改由系統現場量測，拒絕前先釋放所有可重建快取，
  記憶體警告不再先收掉使用者正在看的面板；拒絕時顯示實際用量／上限
- emoji 面板記憶體只增不減 — 主動清 CoreText 字形快取（同 SwiftKey 做法）
- 英打模式切到注音／拼音查碼時，空白與退格改由查碼模式優先處理
- 候選列與鍵面之間的縫也能捲／點候選；工具列／候選字／鍵面外緣的觸控死角
- ⚙ 面板「開啟設定」多試幾條開啟路徑（iOS 27 起 SwiftUI Link 不通），全部失敗時提示手動開啟

### 新增
- 單字聯想 5→10 個；候選列右緣加 › 溢出指示
- 隱私權政策頁（App Store 上架必填）

## [0.4.0] - 2026-08-25

### 新增
- 常用語可設自訂組字碼 — 常用語設定頁每列「詞＋組字碼」，打碼直接叫出、不必開 ♥ 面板；
  碼已被字表使用時本詞排在既有候選之後（設定時即時提示 ✓／⚠／✗）。`user_phrases.txt`
  改為「詞<TAB>碼」格式，與 Android 版互通
- 密碼與 asciiCapable 欄位自動暫切英文直通，離開欄位還原（不影響記住的中英模式）
- 「米模式字母鍵顯示大寫」偏好
- 按鍵按下時可另設邊框色與邊框寬（同 Android 版）

### 變更
- 效能：注音／拼音／字頻改讀 mmap 二進位（ZYM2／PYM2／CFM2）— Resources 610 KB JSON → 172 KB，
  extension 零 heap；`phrases.bin` 改 PHM2 格式 687→274 KB，與 Android 版共用同一份
- 容器 app 不再隨附資料檔（只有鍵盤 extension 需要），.app 3,028→2,492 KB
- App Store 上傳前置：iPad 補第四個旋轉方向、標示不使用自訂加密

## [0.3.0] - 2026-08-20

### 新增
- 成對標點擴到半形：`(` `[` `{` 與雙引號也自動補右半、游標停在中間（單引號刻意不配 —
  don't／Daniel's 會被誤補）；符號頁／符號面板／按鍵上下滑按出來的成對標點也會補，
  不再只有打碼選字那條路徑會配對
- 工具列 ⚙ 展開指令面板 — `,,` 指令變成動作鈕，不必記指令字串
- 聯想詞列左側 ✕ 關閉鈕 — 不想要聯想時一鍵回到工具列
- 組字碼可點 — 有候選時也能點左側字母原樣上屏（同 Android 版）
- 英文直通 — 無候選時續打不清除、空白鍵原樣送出；滿碼頂字上屏改為偏好（預設關）
- 表情面板新增「常用」分類（最近使用的 emoji）
- 皮膚區新增鍵盤外觀編輯器連結；`.cskin` 檔案關聯，點檔案詢問後套用

### 變更
- iOS 27 液態玻璃適配 — 背板交給系統玻璃，鍵面與內容壓成不透明
- 效能：鍵擊路徑零 SQLite、主題與視圖快取（sweetlime 移植，同 Android 版）
- 記憶體：面板收鍵盤即拆、接系統記憶體警告、分類延後取值、繁簡表改 lazy
- 移除 Debug 記錄功能（設定 toggle 與全部呼叫點）
- 中英切換不再彈出鍵盤中央的模式提示
- 候選列移除隱式淡入淡出（`.system` 按鈕 setTitle 的預設 crossfade 會拖慢顯示）
- 用語與 Android 版一致：「自訂詞」改稱「常用語設定」、「皮膚設計器」改稱「鍵盤外觀編輯器」

### 修正
- 深淺色切換偶發卡在舊主題 — 色彩 provider 改為 traits 純函數
- `,,H` 說明移除 yabomish 殘留（拖拉調高度、網格展開、剪貼簿貼上鍵皆非本平台功能）

## [0.2.0] - 2026-08-15

### 新增
- 鍵盤高度縮放（85–140%，設定頁滑桿；同 Android 版）

### 變更
- 空白鍵最大化（同 Android 版）：工具列含 123（按鈕 ID 9/29）時底列不再重複放 123 鍵，
  寬度讓給空白鍵；逗號句號固定標準鍵寬，不再採用皮膚 spaceKeyLayout 放大值
- iOS 不需 Android 版的「隱藏 🌐 鍵」設定與米/英長按 — 系統本就提供切換輸入法的地球鍵，
  extension 內的 🌐 鍵已依 `needsInputModeSwitchKey` 自動隱藏

## [0.1.0] - 2026-08-14

### 新增
- 從 Yabomish `Sources/Shared/`（未發布 iOS 實作）抽出引擎，iOS 側 `#if os(iOS)` 實體化
- 鍵盤 extension：字母／數字／符號／注音四頁、候選列、聯想詞列、toast／字根提示
- 容器 app：匯入 liu.cin（on-device 編譯）、偏好設定、自訂詞編輯
- 基本聯想詞：萌典詞組（phrases.bin，CC0）＋ user_phrases.txt，`,,SG` 開關
- 字頻學習（freq.db）＋ `,,PIN` 固定排序、注音／拼音／同音查碼、`,,` 指令系統
- 引擎測試（macOS host swiftc 執行）
